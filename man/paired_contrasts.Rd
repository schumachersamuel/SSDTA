% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/did_paired.R
\name{paired_contrasts}
\alias{paired_contrasts}
\title{Per-study paired sensitivity contrast and its SE, from 2x2 counts}
\usage{
paired_contrasts(tp1, fn1, tp2, fn2, rho = 0, correct = TRUE)
}
\arguments{
\item{tp1, fn1}{true-positive / false-negative counts for test 1, per study.}

\item{tp2, fn2}{true-positive / false-negative counts for test 2, per study
(same length, same study order).}

\item{rho}{assumed within-study correlation (scalar, or vector recycled to
study length).}

\item{correct}{apply a Haldane-Anscombe continuity correction
(p_c = (tp+0.5)/(n+1)) to the SE -- but not the point estimate -- whenever
a study has perfect (100%) sensitivity on either test. Without this, a
perfect-sensitivity study gets SE = 0, which assigns it infinite weight in
any downstream meta-analysis (breaks tau^2 estimation). Default TRUE.}
}
\value{
data.frame with delta, se, se1, se2, n1, n2 per study.
}
\description{
delta_i = Se(test1)_i - Se(test2)_i, with
Var(delta_i) = Var(Se1) + Var(Se2) - 2*rho*SD(Se1)*SD(Se2)
(marginal binomial SEs; rho supplies the within-pair correlation that
the two separate 2x2 tables cannot identify on their own).
}
